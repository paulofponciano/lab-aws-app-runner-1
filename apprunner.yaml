version: 1.0
runtime: python3
build:
  commands:
    build:
    - pip install pipenv
    - pipenv install
    - pip install -r requirements.txt
run:
  runtime-version: 3.7.7
  command: python productpage.py
  network:
    port: 9080