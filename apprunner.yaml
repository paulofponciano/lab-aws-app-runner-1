version: 1.0
runtime: python3
build:
  commands:
    build:
    - pip install pipenv
    - pipenv install
    - pip install -r requirements.txt
run:
  command: python productpage.py
  network:
    port: 9080